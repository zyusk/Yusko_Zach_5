# Copy to .env (gitignored, never commit the real key) and fill in.
# Get a free key by POSTing your email to https://fraser.stlouisfed.org/api/api_key
# -- see docs/ai_prompts.txt for the exact working curl_cffi call.
FRASER_API_KEY=
